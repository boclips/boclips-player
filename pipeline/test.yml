---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: node
    tag: '22.13.1-slim'

inputs:
- name: source

run:
  path: source/pipeline/test.sh

caches:
  - path: ../../../root/.cache
  - path: ../../../root/.npm
